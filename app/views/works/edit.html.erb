<div class="row first-row">
  <div class="small-12 medium-12 large-12 small-centered columns">
    <h1>Edit Project</h1>

    <p>
      <%= link_to("← All Projects", works_path) %> | <%= link_to 'Show Project', work_path(@work.id) %>
    </p>

    <%= render 'form' %>
  </div>
</div>